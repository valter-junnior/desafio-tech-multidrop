utilizando agora o products como base que ja foi migrado para a estrutura de application, core e infra migre o /sales para lá também seguindo como exemplo e refatorando/criando os arquivos necessário para a arquitetura

use o `nvm use 24` para o node