agora crie o frontend do projeto implementado no backend no /frontend utilizando react + ts + tailwind + shadcn, implemente as funcoes basicas que ja tem no backend para o frontend e utilize outras libs como zod, react query, react router, tanstack table

utlize uma arquitetura de 

shared/
    - componetnts
    - libs
    - hooks
layout/
app/
    - config/
    - data/
    - routes/
    - services/
features/ 
  - product/
    - pages/
    - components
    - hooks
    - ....

use `nvm use 24` para o node